## Install

```
git clone git@github.com:livingobjects/ionic-hybrid-client-demo.git
cd ionic-hybrid-client-demo
sudo npm install -g cordova ionic
npm install

```

## Run it

```
ionic platform add ios
ionic build ios
sudo npm install -g ios-sim
ionic emulate ios
```
